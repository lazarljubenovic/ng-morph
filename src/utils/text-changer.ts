import { Token } from '../nodes/ng-ast-node/template/tokenizer/lexer';
import { LocationSpan } from '../nodes/ng-ast-node/location';

export function changeText(
  newText: string,
  tokens: Token[],
  sourceSpan: LocationSpan
) {}
